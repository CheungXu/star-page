# PostgreSQL/RDS 连接配置模板。
# 复制为 config/db.env 后填写真实值；config/*.env 已被忽略，不要提交真实 db.env。

# 优先使用 RDS 内网地址。如果轻量服务器无法内网访问，再使用公网地址并限制白名单。
PGHOST=
PGPORT=5432
PGDATABASE=postgres
PGUSER=
PGPASSWORD=

# 也可以改用完整连接串。若填写 DATABASE_URL，测试脚本会优先使用它。
DATABASE_URL=
